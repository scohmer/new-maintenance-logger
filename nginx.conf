server {
    listen 80;
    server_name localhost maintenancelogs.seancohmer.com;

    # Security headers (without HSTS for HTTP)
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    # Proxy to FastAPI application
    location / {
        proxy_pass         http://maintenance-logger:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   X-Forwarded-Host $server_name;
        
        # WebSocket support (if needed in future)
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout       60s;
        proxy_send_timeout          60s;
        proxy_read_timeout          60s;
    }

    # Health check endpoint
    location /health {
        access_log off;
        proxy_pass http://maintenance-logger:8000/log;
        proxy_set_header Host $host;
    }
}

# Optional: Add HTTPS redirect server block when SSL is available
# server {
#     listen 443 ssl http2;
#     server_name maintenancelogs.seancohmer.com;
#     
#     ssl_certificate     /etc/ssl/certs/maintenancelogs.seancohmer.com.crt;
#     ssl_certificate_key /etc/ssl/certs/maintenancelogs.seancohmer.com.key;
#     
#     # Include same proxy configuration as above
# }